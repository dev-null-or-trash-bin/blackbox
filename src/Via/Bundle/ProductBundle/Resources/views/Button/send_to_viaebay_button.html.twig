{#% extends 'SonataAdminBundle:CRUD:list__action.html.twig' %#}

{#% if admin.hasRoute('via_ebay_action') %#}
<div class="btn-group">
    <a id="{{ admin.id(object) }}" href="{{ admin.generateObjectUrl('sendToBlackbox', object) }}" class="btn btn-small sonata-ba-action" onclick="return start_field_dialog_form_add_via_ebay(this);" title="{{ 'label.sendToBlackbox'|trans({}) }}">
        <i class="icon-upload"></i>
        {{ 'label.sendToBlackbox'|trans({}) }}
    </a>
</div>
{#% endif %#}